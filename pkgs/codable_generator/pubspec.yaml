name: codable_generator
description: High-performance single-pass streaming serializer code generator for package:codable.
version: 0.1.0-dev
publish_to: none

environment:
  sdk: ^3.13.0

resolution: workspace

dependencies:
  analyzer: ^7.2.0
  build: ^2.4.1
  codable: ^0.1.0-dev
  source_gen: ^2.0.0

dev_dependencies:
  build_runner: ^2.4.14
  build_test: ^2.2.2
  checks: ^0.3.0
  dart_flutter_team_lints: ^3.0.0
  test: ^1.25.8
