name: codable
description: Next-generation high-performance zero-allocation serialization framework for Dart.
version: 0.1.0-dev
publish_to: none

environment:
  sdk: ^3.13.0

resolution: workspace

dependencies:
  meta: ^1.15.0

dev_dependencies:
  build_runner: ^2.4.14
  build_verify: ^3.1.0
  checks: ^0.3.0
  codable_generator: ^0.1.0-dev
  dart_flutter_team_lints: ^3.0.0
  test: ^1.25.8
