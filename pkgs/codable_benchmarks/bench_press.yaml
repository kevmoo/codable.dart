version: 1
defaults:
  targets: [aot, wasm, js]
  trials: 15
matrix:
  baseline:
    sdk: native_kernels
    runtime: aot
  axes:
    sdk:
      native_kernels: "~/.local/share/dart-sdk-json-utf8-kernels/dart-sdk"
    runtime:
      - aot
      - wasm
      - js
